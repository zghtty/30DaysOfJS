// 练习2：声明函数fullName，接收firstName和lastName参数，返回全名
function fullName(firstName, lastName) {
  return `${firstName} ${lastName}`;
}

// 调用函数（示例：firstName为"Li"，lastName为"Si"）
const myFullName = fullName("Li", "Si");
console.log("我的全名：", myFullName);